<li class="a-burger">
    <span class="burger-name">{{burger_name}}</span>

    <button class="change-devour" data-id="{{id}}" data-devouredvariable="{{devouredVariable}}">
        {{#if devoured}}Make this again!{{else}}DEVOUR BURGER!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>